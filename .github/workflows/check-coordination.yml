name: Check coordination syntax
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/check-coordination.yml'
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check coordination JavaScript
        run: node --check coordination.js
